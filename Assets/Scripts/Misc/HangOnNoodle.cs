using UnityEngine;
using System.Collections;

public class HangOnNoodle : MonoBehaviour {

	/*void OnTriggerEnter(Collider other) {
		if(other.CompareTag("Player")){
			Player_Physics phys = other.GetComponent<Player_Physics>();
			phys.Hanging = true;
		}
	}

	void OnTriggerExit(Collider other){
		if(other.CompareTag("Player")){
			Player_Physics phys = other.GetComponent<Player_Physics>();
			phys.Hanging = false;
		}
	}*/
}
